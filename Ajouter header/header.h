#ifndef HEADER_H_INCLUDED
#define HEADER_H_INCLUDED

int ajouteDeux(int nombreRecu);

#include <string>

void afficherMessage(std::string message); //des strings il faut ajouter la ligne d'inclusion #include <string> avant le prototype
                    // utilisation de std:: car no namespace

#endif // HEADER_H_INCLUDED
